# Fill out the app secrets below and remove the "_proto" from the filename

# These keys much match the ones in appsecrets.js
admin_site_key = "foo"
user_site_key = "bar"
static_key = "foobar"

# set to "prod" to eliminate the API Docs standard to FastAPI
app_mode = "test"

# Must be valid sqlAConnectionString
sqlAConnectionString = "sqlite://..."
